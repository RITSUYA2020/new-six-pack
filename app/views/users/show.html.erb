<div class="wrap">
	<div class="container-fluid">
		<%= render 'layouts/flashes' %>
		<div class= "row ml-1 mt-2">
			<h2>マイページ</h2>
		</div>
			<%= render 'layouts/profile', user: @user %>
			<%= render 'work_outs/lists', work_outs: @work_outs %>
	</div>
</div>
